name: T073 Closeout

on:
  push:
    branches:
      - closeout/001-t073-explicit-remote-http-reasoner

permissions:
  contents: write

jobs:
  closeout:
    name: Record canonical T073 completion
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout closeout branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: closeout/001-t073-explicit-remote-http-reasoner
      - name: Mark T073 complete once
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          path = Path('specs/001-v0-1-evidence-guard-foundation/tasks.md')
          text = path.read_text(encoding='utf-8')
          old = '- [ ] T073 [P] [US5] Implement generic explicit remote HTTP adapter without provider SDK authority; whole-repo upload prohibited by default.'
          new = '- [x] T073 [P] [US5] Implement generic explicit remote HTTP adapter without provider SDK authority; whole-repo upload prohibited by default.'
          if new in text:
              raise SystemExit(0)
          if text.count(old) != 1:
              raise SystemExit('T073 task line missing or ambiguous')
          path.write_text(text.replace(old, new), encoding='utf-8')
          PY
          if git diff --quiet -- specs/001-v0-1-evidence-guard-foundation/tasks.md; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add specs/001-v0-1-evidence-guard-foundation/tasks.md
          git commit -m 'docs(spec): mark T073 complete'
          git push origin HEAD:closeout/001-t073-explicit-remote-http-reasoner
