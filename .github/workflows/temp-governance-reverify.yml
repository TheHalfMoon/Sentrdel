name: Temporary Governance Reverify

on:
  push:
    branches:
      - ci/temp-governance-reverify-20260901

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Verify live repository governance
        env:
          GITHUB_TOKEN: ${{ secrets.SENTRDEL_GOVERNANCE_ADMIN_TOKEN }}
        run: python3 scripts/verify_repository_governance.py

# Probe target main after canonical R2-T016 closeout: 2f6686029572f46794b866e0f46d26d63cc4d3a9
# Reverified for continuation at 2026-09-01T00:31+03:00
