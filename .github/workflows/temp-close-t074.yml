name: Temporary T074 Closeout

on:
  push:
    branches:
      - closeout/001-t074-reasoner-evidence-mapping

permissions:
  contents: write

jobs:
  closeout:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          persist-credentials: true
          fetch-depth: 1
      - name: Apply exact checkbox-only closeout and remove temporary workflow
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          path = Path('specs/001-v0-1-evidence-guard-foundation/tasks.md')
          text = path.read_text(encoding='utf-8')
          old = '- [ ] T074 [US5] Strictly map reasoner output to INFERENCE/HYPOTHESIS Evidence.'
          new = '- [x] T074 [US5] Strictly map reasoner output to INFERENCE/HYPOTHESIS Evidence.'
          if text.count(old) != 1:
              raise SystemExit('T074 open checkbox not found exactly once')
          path.write_text(text.replace(old, new), encoding='utf-8')
          PY
          rm .github/workflows/temp-close-t074.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add specs/001-v0-1-evidence-guard-foundation/tasks.md .github/workflows/temp-close-t074.yml
          git commit -m 'docs(spec): close T074 reasoner Evidence mapping'
          git push origin HEAD:closeout/001-t074-reasoner-evidence-mapping
